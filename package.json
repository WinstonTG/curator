{
  "name": "curator-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web",
    "scripts"
  ],
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "check:metrics": "tsx analytics/guards/metric_integrity_check.ts",
    "generate:events": "pnpm -F @curator/scripts generate-events",
    "connectors:sync": "tsx packages/connectors/src/cli.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0",
    "yaml": "^2.3.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
