{
  "name": "@curator/types",
  "version": "0.1.0",
  "description": "Shared TypeScript types for Curator platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "echo 'No tests for types package' && exit 0"
  },
  "keywords": ["curator", "types", "typescript"],
  "author": "Curator Team",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
